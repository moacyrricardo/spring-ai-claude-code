<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.iskeru</groupId>
		<artifactId>claudecode-p-parent</artifactId>
		<version>0.1.0-SNAPSHOT</version>
	</parent>

	<artifactId>spring-ai-claudecode-p</artifactId>
	<name>Spring AI Claude Code ChatModel</name>
	<description>A Spring AI 2.0 ChatModel backed by the local `claude -p` CLI, for token-free test suites</description>

	<properties>
		<spring-ai.version>2.0.0</spring-ai.version>
		<spring-boot.version>4.1.0</spring-boot.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-dependencies</artifactId>
				<version>${spring-boot.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<!-- The CLI transport, response envelope and record/replay layer. -->
		<dependency>
			<groupId>com.iskeru</groupId>
			<artifactId>claudecode-cli</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- The ChatModel/Prompt/ChatResponse types we implement against. -->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-client-chat</artifactId>
		</dependency>

		<!--
		  Spring Boot auto-configuration is optional: the library works as a plain
		  `new ClaudeCodeChatModel(...)` outside Spring entirely.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- `RecordingFakeCli`, shared by the core's tests and both adapters'. -->
		<dependency>
			<groupId>com.iskeru</groupId>
			<artifactId>claudecode-cli</artifactId>
			<version>${project.version}</version>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!--
			  Everything except `ClaudeCodeChatOptions` and `ClaudeCodeUsage` is shared
			  verbatim with the 1.x adapter; those two are the only classes whose Spring AI
			  supertypes genuinely differ between generations.
			-->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>add-shared-adapter-sources</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>add-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>${project.basedir}/../spring-ai-adapter/src/main/java</source>
							</sources>
						</configuration>
					</execution>
					<execution>
						<id>add-shared-adapter-test-sources</id>
						<phase>generate-test-sources</phase>
						<goals>
							<goal>add-test-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>${project.basedir}/../spring-ai-adapter/src/test/java</source>
							</sources>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
