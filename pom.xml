<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.iskeru</groupId>
	<artifactId>claudecode-p-parent</artifactId>
	<version>0.1.0-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>Claude Code -p (parent)</name>
	<description>Reactor for the Spring-free Claude Code CLI core and its Spring AI adapters</description>

	<modules>
		<module>claudecode-cli</module>
		<module>spring-ai-claudecode-p</module>
	</modules>

	<properties>
		<java.version>17</java.version>
		<maven.compiler.release>${java.version}</maven.compiler.release>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<!-- Only the core pins Jackson directly; each adapter takes it from its own Boot BOM. -->
		<jackson.version>2.20.0</jackson.version>
		<assertj.version>3.27.6</assertj.version>
		<junit.version>5.14.0</junit.version>

		<build-helper.version>3.6.1</build-helper.version>

		<!-- Overridden by the `live` profile; see the surefire config below. -->
		<surefire.groups></surefire.groups>
		<surefire.excludedGroups>live</surefire.excludedGroups>
	</properties>

	<profiles>
		<!--
		  `mvn test -Plive` runs only the tests that shell out to a real, authenticated
		  `claude` binary. They consume subscription usage, so they are off by default.
		-->
		<profile>
			<id>live</id>
			<properties>
				<surefire.groups>live</surefire.groups>
				<surefire.excludedGroups></surefire.excludedGroups>
			</properties>
		</profile>
	</profiles>

	<dependencyManagement>
		<dependencies>
			<!--
			  Test tooling is pinned here for the whole reactor. Spring versions are not:
			  each adapter imports its own Spring AI / Spring Boot BOM, which is the entire
			  point of the split.
			-->
			<dependency>
				<groupId>org.junit</groupId>
				<artifactId>junit-bom</artifactId>
				<version>${junit.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.assertj</groupId>
				<artifactId>assertj-core</artifactId>
				<version>${assertj.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>build-helper-maven-plugin</artifactId>
					<version>${build-helper.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-enforcer-plugin</artifactId>
					<version>3.5.0</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-jar-plugin</artifactId>
					<version>3.4.2</version>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.14.0</version>
				<configuration>
					<release>${java.version}</release>
					<compilerArgs>
						<arg>-parameters</arg>
					</compilerArgs>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.5.4</version>
				<configuration>
					<!--
					  Tests that shell out to a real `claude` binary are tagged "live" and
					  excluded by default. Run them with: mvn test -Plive
					-->
					<groups>${surefire.groups}</groups>
					<excludedGroups>${surefire.excludedGroups}</excludedGroups>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
