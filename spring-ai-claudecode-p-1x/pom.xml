<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.iskeru</groupId>
		<artifactId>claudecode-p-parent</artifactId>
		<version>0.1.0-SNAPSHOT</version>
	</parent>

	<artifactId>spring-ai-claudecode-p-1x</artifactId>
	<name>Spring AI 1.1 Claude Code ChatModel</name>
	<description>The same ChatModel for applications still on Spring AI 1.1.x / Spring Boot 3.5.x</description>

	<properties>
		<!-- There is no Spring AI 1.4; 1.1.8 is the latest 1.x release. -->
		<spring-ai.version>1.1.8</spring-ai.version>
		<spring-boot.version>3.5.16</spring-boot.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-dependencies</artifactId>
				<version>${spring-boot.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<!-- The CLI transport, response envelope and record/replay layer. -->
		<dependency>
			<groupId>com.iskeru</groupId>
			<artifactId>claudecode-cli</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- The ChatModel/Prompt/ChatResponse types we implement against. -->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-client-chat</artifactId>
		</dependency>

		<!--
		  Spring Boot auto-configuration is optional: the library works as a plain
		  `new ClaudeCodeChatModel(...)` outside Spring entirely.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- `RecordingFakeCli`, shared by the core's tests and both adapters'. -->
		<dependency>
			<groupId>com.iskeru</groupId>
			<artifactId>claudecode-cli</artifactId>
			<version>${project.version}</version>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!--
			  The same shared adapter sources the 2.0 module compiles. Only
			  `ClaudeCodeChatOptions` and `ClaudeCodeUsage` live here, under the same
			  fully-qualified names, so the shared classes bind to whichever twin is present.
			-->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>add-shared-adapter-sources</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>add-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>${project.basedir}/../spring-ai-adapter/src/main/java</source>
							</sources>
						</configuration>
					</execution>
					<execution>
						<id>add-shared-adapter-test-sources</id>
						<phase>generate-test-sources</phase>
						<goals>
							<goal>add-test-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>${project.basedir}/../spring-ai-adapter/src/test/java</source>
							</sources>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
